# macos-setup lean alignment plan

- [ ] Replace the current brittle component framework with a lean installer centered on dotfiles `setup.sh` and `Brewfile`.
- [ ] Add profile behavior so defaults are practical (`Standard`), with optional extras in `Full`.
- [ ] Ensure macOS dotfiles links are applied from the dotfiles repo subset only.
- [ ] Update README with one-liner and profile usage.
- [ ] Verify script syntax and profile command behavior.

## Review

- Pending
